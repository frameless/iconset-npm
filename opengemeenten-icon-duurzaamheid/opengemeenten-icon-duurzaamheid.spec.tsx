import { OpengemeentenIconDuurzaamheid } from './opengemeenten-icon-duurzaamheid';
describe('OpengemeentenIconDuurzaamheid', () => {
  it('builds', () => {
    expect(new OpengemeentenIconDuurzaamheid()).toBeTruthy();
  });
});
