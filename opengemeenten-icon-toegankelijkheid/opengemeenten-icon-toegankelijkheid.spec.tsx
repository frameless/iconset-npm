import { OpengemeentenIconToegankelijkheid } from './opengemeenten-icon-toegankelijkheid';
describe('OpengemeentenIconToegankelijkheid', () => {
  it('builds', () => {
    expect(new OpengemeentenIconToegankelijkheid()).toBeTruthy();
  });
});
