import type { Components, JSX } from "../types/components";

interface OpengemeentenIconAgenda extends Components.OpengemeentenIconAgenda, HTMLElement {}
export const OpengemeentenIconAgenda: {
  prototype: OpengemeentenIconAgenda;
  new (): OpengemeentenIconAgenda;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
