import type { Components, JSX } from "../types/components";

interface OpengemeentenIconVrijwilligerswerk extends Components.OpengemeentenIconVrijwilligerswerk, HTMLElement {}
export const OpengemeentenIconVrijwilligerswerk: {
  prototype: OpengemeentenIconVrijwilligerswerk;
  new (): OpengemeentenIconVrijwilligerswerk;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
