import type { Components, JSX } from "../types/components";

interface OpengemeentenIconWandelen extends Components.OpengemeentenIconWandelen, HTMLElement {}
export const OpengemeentenIconWandelen: {
  prototype: OpengemeentenIconWandelen;
  new (): OpengemeentenIconWandelen;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
