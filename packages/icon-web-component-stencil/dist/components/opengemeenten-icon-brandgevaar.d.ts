import type { Components, JSX } from "../types/components";

interface OpengemeentenIconBrandgevaar extends Components.OpengemeentenIconBrandgevaar, HTMLElement {}
export const OpengemeentenIconBrandgevaar: {
  prototype: OpengemeentenIconBrandgevaar;
  new (): OpengemeentenIconBrandgevaar;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
