import { OpengemeentenIconAfvalScheiden } from './opengemeenten-icon-afval-scheiden';
describe('OpengemeentenIconAfvalScheiden', () => {
  it('builds', () => {
    expect(new OpengemeentenIconAfvalScheiden()).toBeTruthy();
  });
});
