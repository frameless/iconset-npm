import { OpengemeentenIconHondenbelasting } from './opengemeenten-icon-hondenbelasting';
describe('OpengemeentenIconHondenbelasting', () => {
  it('builds', () => {
    expect(new OpengemeentenIconHondenbelasting()).toBeTruthy();
  });
});
