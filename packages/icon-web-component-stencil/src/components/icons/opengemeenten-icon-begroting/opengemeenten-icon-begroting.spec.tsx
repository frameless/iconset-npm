import { OpengemeentenIconBegroting } from './opengemeenten-icon-begroting';
describe('OpengemeentenIconBegroting', () => {
  it('builds', () => {
    expect(new OpengemeentenIconBegroting()).toBeTruthy();
  });
});
