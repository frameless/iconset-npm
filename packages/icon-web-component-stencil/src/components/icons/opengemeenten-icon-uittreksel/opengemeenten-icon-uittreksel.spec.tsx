import { OpengemeentenIconUittreksel } from './opengemeenten-icon-uittreksel';
describe('OpengemeentenIconUittreksel', () => {
  it('builds', () => {
    expect(new OpengemeentenIconUittreksel()).toBeTruthy();
  });
});
