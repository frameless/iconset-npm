import { OpengemeentenIconSinterklaas } from './opengemeenten-icon-sinterklaas';
describe('OpengemeentenIconSinterklaas', () => {
  it('builds', () => {
    expect(new OpengemeentenIconSinterklaas()).toBeTruthy();
  });
});
