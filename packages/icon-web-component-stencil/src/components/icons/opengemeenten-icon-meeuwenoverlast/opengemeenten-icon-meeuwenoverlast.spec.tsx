import { OpengemeentenIconMeeuwenoverlast } from './opengemeenten-icon-meeuwenoverlast';
describe('OpengemeentenIconMeeuwenoverlast', () => {
  it('builds', () => {
    expect(new OpengemeentenIconMeeuwenoverlast()).toBeTruthy();
  });
});
