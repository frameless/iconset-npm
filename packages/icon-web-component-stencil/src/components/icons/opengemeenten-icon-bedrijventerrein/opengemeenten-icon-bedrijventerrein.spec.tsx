import { OpengemeentenIconBedrijventerrein } from './opengemeenten-icon-bedrijventerrein';
describe('OpengemeentenIconBedrijventerrein', () => {
  it('builds', () => {
    expect(new OpengemeentenIconBedrijventerrein()).toBeTruthy();
  });
});
