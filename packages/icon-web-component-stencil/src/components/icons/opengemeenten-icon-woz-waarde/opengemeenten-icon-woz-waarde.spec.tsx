import { OpengemeentenIconWozWaarde } from './opengemeenten-icon-woz-waarde';
describe('OpengemeentenIconWozWaarde', () => {
  it('builds', () => {
    expect(new OpengemeentenIconWozWaarde()).toBeTruthy();
  });
});
