import { OpengemeentenIconDocumenten } from './opengemeenten-icon-documenten';
describe('OpengemeentenIconDocumenten', () => {
  it('builds', () => {
    expect(new OpengemeentenIconDocumenten()).toBeTruthy();
  });
});
