import type { Components, JSX } from "../types/components";

interface OpengemeentenIconOverlijden extends Components.OpengemeentenIconOverlijden, HTMLElement {}
export const OpengemeentenIconOverlijden: {
  prototype: OpengemeentenIconOverlijden;
  new (): OpengemeentenIconOverlijden;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
