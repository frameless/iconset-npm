import type { Components, JSX } from "../types/components";

interface OpengemeentenIconVuurwerk extends Components.OpengemeentenIconVuurwerk, HTMLElement {}
export const OpengemeentenIconVuurwerk: {
  prototype: OpengemeentenIconVuurwerk;
  new (): OpengemeentenIconVuurwerk;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
