import type { Components, JSX } from "../types/components";

interface OpengemeentenIconBedrijventerrein extends Components.OpengemeentenIconBedrijventerrein, HTMLElement {}
export const OpengemeentenIconBedrijventerrein: {
  prototype: OpengemeentenIconBedrijventerrein;
  new (): OpengemeentenIconBedrijventerrein;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
