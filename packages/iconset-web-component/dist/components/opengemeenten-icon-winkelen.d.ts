import type { Components, JSX } from "../types/components";

interface OpengemeentenIconWinkelen extends Components.OpengemeentenIconWinkelen, HTMLElement {}
export const OpengemeentenIconWinkelen: {
  prototype: OpengemeentenIconWinkelen;
  new (): OpengemeentenIconWinkelen;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
