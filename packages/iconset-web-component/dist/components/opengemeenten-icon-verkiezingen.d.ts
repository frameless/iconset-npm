import type { Components, JSX } from "../types/components";

interface OpengemeentenIconVerkiezingen extends Components.OpengemeentenIconVerkiezingen, HTMLElement {}
export const OpengemeentenIconVerkiezingen: {
  prototype: OpengemeentenIconVerkiezingen;
  new (): OpengemeentenIconVerkiezingen;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
