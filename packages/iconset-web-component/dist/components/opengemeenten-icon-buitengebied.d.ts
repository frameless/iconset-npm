import type { Components, JSX } from "../types/components";

interface OpengemeentenIconBuitengebied extends Components.OpengemeentenIconBuitengebied, HTMLElement {}
export const OpengemeentenIconBuitengebied: {
  prototype: OpengemeentenIconBuitengebied;
  new (): OpengemeentenIconBuitengebied;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
