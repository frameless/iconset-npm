import type { Components, JSX } from "../types/components";

interface OpengemeentenIconComputer extends Components.OpengemeentenIconComputer, HTMLElement {}
export const OpengemeentenIconComputer: {
  prototype: OpengemeentenIconComputer;
  new (): OpengemeentenIconComputer;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
