import type { Components, JSX } from "../types/components";

interface OpengemeentenIconVeerboot extends Components.OpengemeentenIconVeerboot, HTMLElement {}
export const OpengemeentenIconVeerboot: {
  prototype: OpengemeentenIconVeerboot;
  new (): OpengemeentenIconVeerboot;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
