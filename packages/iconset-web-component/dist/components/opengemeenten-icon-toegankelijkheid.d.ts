import type { Components, JSX } from "../types/components";

interface OpengemeentenIconToegankelijkheid extends Components.OpengemeentenIconToegankelijkheid, HTMLElement {}
export const OpengemeentenIconToegankelijkheid: {
  prototype: OpengemeentenIconToegankelijkheid;
  new (): OpengemeentenIconToegankelijkheid;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
