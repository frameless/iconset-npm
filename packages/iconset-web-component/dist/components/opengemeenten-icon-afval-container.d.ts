import type { Components, JSX } from "../types/components";

interface OpengemeentenIconAfvalContainer extends Components.OpengemeentenIconAfvalContainer, HTMLElement {}
export const OpengemeentenIconAfvalContainer: {
  prototype: OpengemeentenIconAfvalContainer;
  new (): OpengemeentenIconAfvalContainer;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
