import type { Components, JSX } from "../types/components";

interface OpengemeentenIconSubsidie extends Components.OpengemeentenIconSubsidie, HTMLElement {}
export const OpengemeentenIconSubsidie: {
  prototype: OpengemeentenIconSubsidie;
  new (): OpengemeentenIconSubsidie;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
