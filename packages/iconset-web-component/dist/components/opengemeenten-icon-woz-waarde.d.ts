import type { Components, JSX } from "../types/components";

interface OpengemeentenIconWozWaarde extends Components.OpengemeentenIconWozWaarde, HTMLElement {}
export const OpengemeentenIconWozWaarde: {
  prototype: OpengemeentenIconWozWaarde;
  new (): OpengemeentenIconWozWaarde;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
