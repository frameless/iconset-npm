import type { Components, JSX } from "../types/components";

interface OpengemeentenIconVoeding extends Components.OpengemeentenIconVoeding, HTMLElement {}
export const OpengemeentenIconVoeding: {
  prototype: OpengemeentenIconVoeding;
  new (): OpengemeentenIconVoeding;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
