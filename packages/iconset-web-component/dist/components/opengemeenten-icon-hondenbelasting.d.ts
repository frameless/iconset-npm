import type { Components, JSX } from "../types/components";

interface OpengemeentenIconHondenbelasting extends Components.OpengemeentenIconHondenbelasting, HTMLElement {}
export const OpengemeentenIconHondenbelasting: {
  prototype: OpengemeentenIconHondenbelasting;
  new (): OpengemeentenIconHondenbelasting;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
