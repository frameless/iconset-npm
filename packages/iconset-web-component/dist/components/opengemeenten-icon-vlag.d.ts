import type { Components, JSX } from "../types/components";

interface OpengemeentenIconVlag extends Components.OpengemeentenIconVlag, HTMLElement {}
export const OpengemeentenIconVlag: {
  prototype: OpengemeentenIconVlag;
  new (): OpengemeentenIconVlag;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
