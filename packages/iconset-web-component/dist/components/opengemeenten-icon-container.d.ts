import type { Components, JSX } from "../types/components";

interface OpengemeentenIconContainer extends Components.OpengemeentenIconContainer, HTMLElement {}
export const OpengemeentenIconContainer: {
  prototype: OpengemeentenIconContainer;
  new (): OpengemeentenIconContainer;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
