import type { Components, JSX } from "../types/components";

interface OpengemeentenIconBevrijding extends Components.OpengemeentenIconBevrijding, HTMLElement {}
export const OpengemeentenIconBevrijding: {
  prototype: OpengemeentenIconBevrijding;
  new (): OpengemeentenIconBevrijding;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
