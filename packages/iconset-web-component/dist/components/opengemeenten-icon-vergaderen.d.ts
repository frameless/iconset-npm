import type { Components, JSX } from "../types/components";

interface OpengemeentenIconVergaderen extends Components.OpengemeentenIconVergaderen, HTMLElement {}
export const OpengemeentenIconVergaderen: {
  prototype: OpengemeentenIconVergaderen;
  new (): OpengemeentenIconVergaderen;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
