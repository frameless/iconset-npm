import type { Components, JSX } from "../types/components";

interface OpengemeentenIconWmo extends Components.OpengemeentenIconWmo, HTMLElement {}
export const OpengemeentenIconWmo: {
  prototype: OpengemeentenIconWmo;
  new (): OpengemeentenIconWmo;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
