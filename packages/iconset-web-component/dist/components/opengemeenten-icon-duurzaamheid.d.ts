import type { Components, JSX } from "../types/components";

interface OpengemeentenIconDuurzaamheid extends Components.OpengemeentenIconDuurzaamheid, HTMLElement {}
export const OpengemeentenIconDuurzaamheid: {
  prototype: OpengemeentenIconDuurzaamheid;
  new (): OpengemeentenIconDuurzaamheid;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
