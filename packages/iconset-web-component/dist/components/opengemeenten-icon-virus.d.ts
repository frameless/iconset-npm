import type { Components, JSX } from "../types/components";

interface OpengemeentenIconVirus extends Components.OpengemeentenIconVirus, HTMLElement {}
export const OpengemeentenIconVirus: {
  prototype: OpengemeentenIconVirus;
  new (): OpengemeentenIconVirus;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
