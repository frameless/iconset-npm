import { OpengemeentenIconContainer } from './opengemeenten-icon-container';

describe('opengemeenten-icon-container', () => {
  it('builds', () => {
    expect(new OpengemeentenIconContainer()).toBeTruthy();
  });
});
