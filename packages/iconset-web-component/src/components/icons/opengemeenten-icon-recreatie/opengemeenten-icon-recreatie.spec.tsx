import { OpengemeentenIconRecreatie } from './opengemeenten-icon-recreatie';
describe('OpengemeentenIconRecreatie', () => {
  it('builds', () => {
    expect(new OpengemeentenIconRecreatie()).toBeTruthy();
  });
});
