import { OpengemeentenIconVrijwilligerswerk } from './opengemeenten-icon-vrijwilligerswerk';
describe('OpengemeentenIconVrijwilligerswerk', () => {
  it('builds', () => {
    expect(new OpengemeentenIconVrijwilligerswerk()).toBeTruthy();
  });
});
