import { OpengemeentenIconBrandgevaar } from './opengemeenten-icon-brandgevaar';
describe('OpengemeentenIconBrandgevaar', () => {
  it('builds', () => {
    expect(new OpengemeentenIconBrandgevaar()).toBeTruthy();
  });
});
