import { OpengemeentenIconWijkteam } from './opengemeenten-icon-wijkteam';
describe('OpengemeentenIconWijkteam', () => {
  it('builds', () => {
    expect(new OpengemeentenIconWijkteam()).toBeTruthy();
  });
});
