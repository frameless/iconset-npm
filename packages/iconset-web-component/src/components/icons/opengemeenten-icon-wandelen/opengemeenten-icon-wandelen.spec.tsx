import { OpengemeentenIconWandelen } from './opengemeenten-icon-wandelen';
describe('OpengemeentenIconWandelen', () => {
  it('builds', () => {
    expect(new OpengemeentenIconWandelen()).toBeTruthy();
  });
});
