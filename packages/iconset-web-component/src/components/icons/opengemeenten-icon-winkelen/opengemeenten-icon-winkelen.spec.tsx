import { OpengemeentenIconWinkelen } from './opengemeenten-icon-winkelen';
describe('OpengemeentenIconWinkelen', () => {
  it('builds', () => {
    expect(new OpengemeentenIconWinkelen()).toBeTruthy();
  });
});
