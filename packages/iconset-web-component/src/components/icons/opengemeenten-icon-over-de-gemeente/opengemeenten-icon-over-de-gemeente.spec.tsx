import { OpengemeentenIconOverDeGemeente } from './opengemeenten-icon-over-de-gemeente';
describe('OpengemeentenIconOverDeGemeente', () => {
  it('builds', () => {
    expect(new OpengemeentenIconOverDeGemeente()).toBeTruthy();
  });
});
