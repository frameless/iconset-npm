import { OpengemeentenIconOmgevingsvergunning } from './opengemeenten-icon-omgevingsvergunning';
describe('OpengemeentenIconOmgevingsvergunning', () => {
  it('builds', () => {
    expect(new OpengemeentenIconOmgevingsvergunning()).toBeTruthy();
  });
});
