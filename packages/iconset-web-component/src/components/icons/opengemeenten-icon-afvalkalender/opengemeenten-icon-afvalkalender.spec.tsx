import { OpengemeentenIconAfvalkalender } from './opengemeenten-icon-afvalkalender';
describe('OpengemeentenIconAfvalkalender', () => {
  it('builds', () => {
    expect(new OpengemeentenIconAfvalkalender()).toBeTruthy();
  });
});
