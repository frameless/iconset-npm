import { OpengemeentenIconGemivaLocatie } from './opengemeenten-icon-gemiva-locatie';
describe('OpengemeentenIconGemivaLocatie', () => {
  it('builds', () => {
    expect(new OpengemeentenIconGemivaLocatie()).toBeTruthy();
  });
});
