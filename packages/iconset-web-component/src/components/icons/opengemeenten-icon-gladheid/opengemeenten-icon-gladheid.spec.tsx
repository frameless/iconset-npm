import { OpengemeentenIconGladheid } from './opengemeenten-icon-gladheid';
describe('OpengemeentenIconGladheid', () => {
  it('builds', () => {
    expect(new OpengemeentenIconGladheid()).toBeTruthy();
  });
});
