import { OpengemeentenIconComputer } from './opengemeenten-icon-computer';
describe('OpengemeentenIconComputer', () => {
  it('builds', () => {
    expect(new OpengemeentenIconComputer()).toBeTruthy();
  });
});
