import { OpengemeentenIconVirus } from './opengemeenten-icon-virus';
describe('OpengemeentenIconVirus', () => {
  it('builds', () => {
    expect(new OpengemeentenIconVirus()).toBeTruthy();
  });
});
