import { OpengemeentenIconLetOp } from './opengemeenten-icon-let-op';
describe('OpengemeentenIconLetOp', () => {
  it('builds', () => {
    expect(new OpengemeentenIconLetOp()).toBeTruthy();
  });
});
