import { OpengemeentenIconGeluid } from './opengemeenten-icon-geluid';
describe('OpengemeentenIconGeluid', () => {
  it('builds', () => {
    expect(new OpengemeentenIconGeluid()).toBeTruthy();
  });
});
