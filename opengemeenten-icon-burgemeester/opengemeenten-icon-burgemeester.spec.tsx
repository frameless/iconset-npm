import { OpengemeentenIconBurgemeester } from './opengemeenten-icon-burgemeester';
describe('OpengemeentenIconBurgemeester', () => {
  it('builds', () => {
    expect(new OpengemeentenIconBurgemeester()).toBeTruthy();
  });
});
