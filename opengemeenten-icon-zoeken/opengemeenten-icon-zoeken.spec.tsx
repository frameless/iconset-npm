import { OpengemeentenIconZoeken } from './opengemeenten-icon-zoeken';
describe('OpengemeentenIconZoeken', () => {
  it('builds', () => {
    expect(new OpengemeentenIconZoeken()).toBeTruthy();
  });
});
