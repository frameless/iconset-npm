import { OpengemeentenIconFiets } from './opengemeenten-icon-fiets';
describe('OpengemeentenIconFiets', () => {
  it('builds', () => {
    expect(new OpengemeentenIconFiets()).toBeTruthy();
  });
});
