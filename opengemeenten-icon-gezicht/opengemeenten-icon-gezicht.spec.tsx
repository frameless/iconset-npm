import { OpengemeentenIconGezicht } from './opengemeenten-icon-gezicht';
describe('OpengemeentenIconGezicht', () => {
  it('builds', () => {
    expect(new OpengemeentenIconGezicht()).toBeTruthy();
  });
});
