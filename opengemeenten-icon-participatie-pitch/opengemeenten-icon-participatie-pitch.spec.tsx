import { OpengemeentenIconParticipatiePitch } from './opengemeenten-icon-participatie-pitch';
describe('OpengemeentenIconParticipatiePitch', () => {
  it('builds', () => {
    expect(new OpengemeentenIconParticipatiePitch()).toBeTruthy();
  });
});
