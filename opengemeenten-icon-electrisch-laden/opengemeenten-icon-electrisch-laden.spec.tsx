import { OpengemeentenIconElectrischLaden } from './opengemeenten-icon-electrisch-laden';
describe('OpengemeentenIconElectrischLaden', () => {
  it('builds', () => {
    expect(new OpengemeentenIconElectrischLaden()).toBeTruthy();
  });
});
