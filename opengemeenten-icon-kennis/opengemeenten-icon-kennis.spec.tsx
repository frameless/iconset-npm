import { OpengemeentenIconKennis } from './opengemeenten-icon-kennis';
describe('OpengemeentenIconKennis', () => {
  it('builds', () => {
    expect(new OpengemeentenIconKennis()).toBeTruthy();
  });
});
