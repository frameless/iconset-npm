import { OpengemeentenIconBezwaarEnBeroep } from './opengemeenten-icon-bezwaar-en-beroep';
describe('OpengemeentenIconBezwaarEnBeroep', () => {
  it('builds', () => {
    expect(new OpengemeentenIconBezwaarEnBeroep()).toBeTruthy();
  });
});
