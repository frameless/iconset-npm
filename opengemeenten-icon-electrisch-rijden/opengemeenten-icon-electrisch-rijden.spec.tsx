import { OpengemeentenIconElectrischRijden } from './opengemeenten-icon-electrisch-rijden';
describe('OpengemeentenIconElectrischRijden', () => {
  it('builds', () => {
    expect(new OpengemeentenIconElectrischRijden()).toBeTruthy();
  });
});
