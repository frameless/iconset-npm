import { OpengemeentenIconHerdenking } from './opengemeenten-icon-herdenking';
describe('OpengemeentenIconHerdenking', () => {
  it('builds', () => {
    expect(new OpengemeentenIconHerdenking()).toBeTruthy();
  });
});
