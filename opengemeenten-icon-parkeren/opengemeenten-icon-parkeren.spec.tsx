import { OpengemeentenIconParkeren } from './opengemeenten-icon-parkeren';
describe('OpengemeentenIconParkeren', () => {
  it('builds', () => {
    expect(new OpengemeentenIconParkeren()).toBeTruthy();
  });
});
